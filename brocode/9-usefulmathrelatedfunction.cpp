#include <iostream>
#include <cmath>
using namespace std;


int main() {
    double x = 9.1;
    int y = 3;
    double z;
    //double z = max(A::x, B::x);
    //double z = min(x, y);
    //double z = pow(x, y); // 6 mũ 3 // hàm pow() là hàm lũy thừa
    //z = sqrt(x); dấu căn hay mũ 1/2
    //z = abs(-5); dấu giá trị tuyệt đối
    //z = round(x); // round là hàm làm tròn như toán học THPT
    //z = ceil(x); //đối với hàm ceil thì luôn làm tròn lên ko làm tròn xuống 3.2=4
    //z = floor(x); ngược lại so với hàm ceil hàm này luôn làm tròn xuống 3.99=3
    cout << z;
    return 0;
}